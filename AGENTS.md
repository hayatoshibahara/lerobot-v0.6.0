# AGENTS.md

## リポジトリの目的

このリポジトリは、LeRobot v0.6.0 を使って SO-101 のセットアップ、データ収録、ACT の訓練・評価を行うワークショップ資料である。受講者向けの入口と実施順は [`README.md`](./README.md) を正とする。

LeRobot 本体のソースコードは `lerobot/` にある。ワークショップ側の文書と LeRobot 本体を区別し、本体へ変更が必要な場合だけ `lerobot/` を編集する。

## 最初に確認するファイル

1. [`README.md`](./README.md) でワークショップ全体の流れを確認する。
2. 変更対象の文書と、その直前・直後に参照する文書を読む。
3. LeRobot の実装や公式文書を調べる場合は [`structure.md`](./structure.md) を参照する。
4. SO-101 の技術的な判断では [`lerobot/AGENT_GUIDE.md`](./lerobot/AGENT_GUIDE.md) も確認する。

## ワークショップ資料の順序

| 順序 | ファイル | 内容 |
| ---: | --- | --- |
| 1 | [`install.md`](./install.md) | LeRobot のインストールと環境確認 |
| 2 | [`setup.md`](./setup.md) | SO-101 のモーター設定、キャリブレーション、テレオペレーション、カメラ確認 |
| 3 | [`dataset.md`](./dataset.md) | ACT 用データセットの収録と Hugging Face Hub への保存 |
| 4 | [`act.md`](./act.md) | Google Colab での ACT 訓練と SO-101 による評価 |

各文書はこの順に実行する前提で書く。前の文書ですでに説明した内容は短く参照し、同じ手順を重複させない。

## 作業時の基本方針

- 回答・説明・ドキュメントは日本語の常体で記述する。
- ジュニアエンジニアが上から順に実行できる文章と構成にする。
- 結論や実行する操作を先に書き、背景説明、注意事項、例外は必要な分だけ続ける。
- 同じ説明を概要、手順、補足、チェックリストで何度も繰り返さない。
- 「適切に」「必要に応じて」だけで済ませず、判断条件や具体例を示す。
- コマンドはコピーして実行できる形にし、置き換える値、実行場所、対象 OS を明記する。
- 用語と設定値は文書間で統一する。特に SO-101、`my_follower`、`my_leader`、`front`、`wrist`、Hugging Face のリポジトリ ID を一貫させる。
- コードを変更するときは可読性を優先し、必要に応じてジュニアエンジニア向けのコメントや docstring を追加する。
- 変更対象に関係する既存の文書と実装を先に確認し、ユーザーの未コミット変更と既存の設計・命名を尊重する。

## 情報とコマンドの確認方針

- まず、このリポジトリに含まれる LeRobot の文書・実装・`--help` を確認する。
- バージョンによって変わる CLI オプションや既定値は、記憶だけで書かず対象バージョンの実装で確認する。
- 最新情報を調べる場合は、Hugging Face の公式文書、LeRobot リポジトリ、GitHub Issues などの一次情報を優先する。
- GitHub Issues を根拠にする場合は、Open / Closed、対象バージョン、個別環境の事例であることを区別する。
- 実機、Hugging Face Hub、Google Colab で未確認の操作を「動作確認済み」と記載しない。

## ディレクトリの使い分け

- `lerobot/`：LeRobot 本体のソースコード、公式ドキュメント、サンプル、テストを含む。
- `README.md`：ワークショップの概要と受講者が読む資料の順序。
- `install.md`、`setup.md`、`dataset.md`、`act.md`：受講者向けの実行手順。
- `structure.md`：LeRobot 本体で参照すべき文書・実装の案内。
- `windows.md`：Windows 環境に関する補足資料。
- `memo/`：EnvHub、VQ-BeT、リリース調査などの調査メモ。受講者向けの主手順とは分けて扱う。

`lerobot/` 内のコードを変更する場合は、サブモジュール側の [`lerobot/AGENTS.md`](./lerobot/AGENTS.md) を必ず確認する。LeRobot 本体の開発コマンド、アーキテクチャ、テスト方針はそちらを優先する。

## 文書変更後の確認

- Markdown の見出し階層、コードブロック、相対リンクが壊れていないか確認する。
- 手順の前提条件と「次に行うこと」が、前後の資料につながっているか確認する。
- コマンドの変数名、アーム ID、カメラ名、データセット名が文書内と文書間で一致しているか確認する。
- 公開データを扱う手順では、トークン、個人情報、映像、ライセンス、公開範囲の注意を含める。
- 文書だけを変更した場合は、実機テストを行っていないことを必要に応じて明記する。

## コミットメッセージ

コミットを作成する場合は Conventional Commits に準拠し、英語 1 行で記述する。例えば次の形式を使う。

```text
docs: add workshop agent guide
```
